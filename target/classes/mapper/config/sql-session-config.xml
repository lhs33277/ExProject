<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE configuration PUBLIC "-//mybatis.org//DTD Config 3.0//EN" "http://mybatis.org/dtd/mybatis-3-config.dtd">
<configuration>

	<properties resource="/config/globals/globals.properties" />

	<settings>
		<setting name="mapUnderscoreToCamelCase" value="true" />
	</settings>

	<typeAliases>
	<!-- DTO 및 VO alias 설정 -->
		<typeAlias alias="BoardDTO" type="com.itkey.sam.board.dto.BoardDTO"/>
		<typeAlias alias="BoardWriterDTO" type="com.itkey.sam.boardWriter.dto.BoardWriterDTO"/>
		<typeAlias alias="FileDTO" type="com.itkey.sam.file.dto.FileDTO"/>
		<typeAlias alias="AdminDTO" type="com.itkey.sam.admin.dto.AdminDTO"/>
	</typeAliases>

	<!-- src/main/resouces/mapper/mysql 하위 xml 매퍼 설정 (현재 하위 모든 매퍼파일 read하도록 되어있으므로 추가 설정 필요 무) -->
	<!-- <mappers></mappers> -->
	
</configuration>
